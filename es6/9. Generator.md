# Generator
```js
// 模式
var iterator = generator()

// methods
iterator.next()
iterator.throw()
iterator.return()
```

# 一个简单的 Generator
```js
function *pickUpAGirl() {
  // send the greeting message
  var response1 = yield 'Hello, how are you?'
  // log the response message
  console.info(response1)
  var response2
  if(response1) {
    response2 = yield `Glad to meet you!`
  } else {
    // try again
    response2 = yield `Hello, I'm talking to you.`
  }
  if(response2) {
    console.info(response2)
  } else {
    return 'Mission Failed...'
  }
}

// tests
var uglyIterator = pickUpAGirl()
uglyIterator.next()

```

# I/O
```js
var handsomeIterator = pickUpAGirl()
handsomeIterator.next()
handsomeIterator.next(`I'm fine, thx, and U?`)
handsomeIterator.next(`Glad to meet you too!`)
```



